# CLAUDE.md — הקשר פרויקט

פרויקט: סימולציה של שיטת בחירות אזורית-רובנית (חד-נציגית, FPTP) על תוצאות הבחירות לכנסת ה-25 (נובמבר 2022) ברמת קלפי.

## נתונים
- `data/ballots_2022.csv` — קלפי × מפלגות. עמודות מפתח: `שם ישוב`, `סמל ישוב`, `כשרים`, ואז עמודה לכל מפלגה לפי אות הפתק (מחל, פה, ט, כן, שס, ג, ל, עם, ום, אמת, מרצ, ד, ...).
- `data/settlement_coords.csv` — `סמל ישוב` → קואורדינטות ITM (encoding cp1255).
- מיזוג לפי `סמל ישוב`. ~4.29M מצביעים ממוקמים; מעטפות חיצוניות (~459K, 9.6%) נופלות מהמיזוג ומוצאות בכוונה.

## אלגוריתם (`engine.py`)
- `load()` → dict עם X (קואורדינטות ITM), W (מצביעים לקלפי), V (קולות למפלגה), P (שיעורי הצבעה), score (ציר ימין-שמאל), parties, city.
- `solve(data, n, alpha=0)` → מערך שיוך קלפי→מחוז. חלוקה בינארית רקורסיבית: בכל שלב חותכים אזור לשניים לאורך הציר הראשי (PCA), במיקום שמשווה מצביעים. `alpha` מטה את החתך לכיוון הומוגניות פוליטית (ברירת מחדל 0 = קומפקטי/גיאוגרפי).
- שוויון גודל 1–5%, רציפות טובה. `metrics()` נותן זוכים, סטיית גודל, win-share.

## גושים
- ימין = מחל+ט · מרכז-שמאל = פה+כן+אמת+מרצ+ל · חרדי = שס+ג · ערבי = ום+עם+ד.

## ויזואליזציה (`build_geojson5.py`)
- Voronoi של יישובים, חתוך למסכת יבשה (isr ∪ alpha-shape).
- ערים המפוצלות בין מחוזות: תת-Voronoi בתוך תא-העיר, הקצאה קפסיטטיבית (linear_sum_assignment) לפי מרחק למרכז-הכובד הגלובלי של כל מחוז → פרוסות מכוונות.
- `cleanup_fragments()` — מסַפח כל רסיס מנותק למחוז השכן הגובל בו הכי הרבה → כל מחוז גוש רציף יחיד. אימות: 0 חפיפות, 0 חורים.
- פלט: `mapdata.json` עם FeatureCollection ל-n120/n215; לכל מחוז properties: d, v (מצביעים), wp (מפלגה זוכה), wb (גוש), m (מרווח), b (שיעורי גושים), s (יישובים), sn.

## תוצאות ידועות (לאימות)
- n=120 גושים: ימין 49, מרכז-שמאל 45, ערבי 15, חרדי 11.
- n=215 גושים: מרכז-שמאל 88, ימין 83, ערבי 23, חרדי 21.
- שינוי גיאומטריה אסור שישנה מספרים אלה — הם נגזרים מהקלפיות בלבד.

## הסתייגויות
- שטח ≠ קולות (מחוזות מדבריים ענקיים דלילי-אוכלוסין).
- פיצול פנימי של ערים הוא סכמטי (אין גיאוגרפיה תת-עירונית בנתונים).
- החלוקה אינה מקוננת: n שונה = חלוקה שונה.

## אתר חי
https://amitlev.github.io/israel-electoral-districts-2022/ (repo: amitlev/israel-electoral-districts-2022)
